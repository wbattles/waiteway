services:
  waiteway:
    build: .
    ports:
      - "8080:8080"
      - "9090:9090"
    volumes:
      - ./data:/data
    environment:
      - WAITEWAY_ADMIN_USERNAME=admin
      - WAITEWAY_ADMIN_PASSWORD=change-me
    restart: unless-stopped
